# Cleanup Index

## What Was Cleaned
- Root markdown reports moved to `docs/archive/root-markdown/`
- Root logs moved to `artifacts/logs/`
- Root text dumps moved to `artifacts/text-dumps/`
- Root API report JSON moved to `artifacts/reports-json/`

## Why
- Keep root directory focused on executable code and core project files.
- Reduce noise for development, CI, and operations.
- Preserve historical material without deleting source artifacts.

## Important
- Archived documents were moved, not deleted.
- Service source code remains under `microservices/`.
