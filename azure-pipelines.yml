# Azure DevOps Pipeline for AKS Deployment
# This pipeline builds Docker images and deploys to Azure Kubernetes Service (AKS)
trigger:
  branches:
    include:
      - main
      - develop
  paths:
    exclude:
      - '*.md'
      - 'docs/*'

variables:
  - name: ACR_NAME
    value: 'eteliosacr'
  - name: ACR_LOGIN_SERVER
    value: 'eteliosacr-hvawabdbgge7e0fu.azurecr.io'
  - name: AKS_RESOURCE_GROUP
    value: 'Etelios-AKS-RG'
  - name: AKS_CLUSTER_NAME
    value: 'Etelios-AKS'
  - name: NAMESPACE
    value: 'etelios-backend-prod'
  - name: IMAGE_TAG
    value: '$(Build.BuildId)'

stages:
- stage: Build
  displayName: 'Build and Push Docker Images'
  jobs:
  - job: BuildImages
    displayName: 'Build All Service Images'
    pool:
      vmImage: 'ubuntu-latest'
    steps:
    - task: AzureCLI@2
      displayName: 'Clean Docker Cache and Build/Push All Images'
      inputs:
        azureSubscription: 'Azure-Service-Connection'
        scriptType: 'bash'
        scriptLocation: 'inlineScript'
        inlineScript: |
          # Set variables
          ACR_NAME="$(ACR_NAME)"
          ACR_LOGIN_SERVER="$(ACR_LOGIN_SERVER)"
          IMAGE_TAG="$(IMAGE_TAG)"
          
          # Clean Docker cache and system to free up space
          echo "🧹 Cleaning Docker cache and system..."
          docker system prune -af --volumes || echo "⚠️  Warning: Docker prune failed, continuing..."
          docker builder prune -af || echo "⚠️  Warning: Docker builder prune failed, continuing..."
          
          # Show disk space
          echo "📊 Current disk usage:"
          df -h / || echo "⚠️  Could not check disk usage"
          
          # Verify Azure CLI is authenticated
          echo "Verifying Azure CLI authentication..."
          az account show || {
            echo "❌ Error: Azure CLI is not authenticated"
            exit 1
          }
          echo "✅ Azure CLI authenticated"
          
          # Verify ACR exists and is accessible
          echo "Verifying ACR access: $ACR_NAME"
          az acr show --name $ACR_NAME --query name -o tsv || {
            echo "❌ Error: Cannot access ACR $ACR_NAME"
            echo "Please verify:"
            echo "  1. ACR name is correct: $ACR_NAME"
            echo "  2. Service principal has 'Reader' role on ACR"
            exit 1
          }
          echo "✅ ACR is accessible"
          
          # Login to ACR using Azure CLI
          echo "Logging in to ACR: $ACR_NAME"
          az acr login --name $ACR_NAME || {
            echo "❌ Error: Failed to login to ACR"
            echo "Please verify service principal has 'AcrPush' role on ACR"
            echo "Grant permission with:"
            echo "  az role assignment create --assignee <service-principal-id> --role AcrPush --scope /subscriptions/<sub-id>/resourceGroups/<rg>/providers/Microsoft.ContainerRegistry/registries/$ACR_NAME"
            exit 1
          }
          echo "✅ Successfully logged in to ACR: $ACR_LOGIN_SERVER"
          
          # Test ACR access by listing repositories
          echo "Testing ACR push permissions..."
          az acr repository list --name $ACR_NAME --output table || echo "⚠️  Warning: Cannot list repositories, but push may still work"
          
          # Build and push API Gateway
          echo "Building and pushing API Gateway..."
          docker build -t $ACR_LOGIN_SERVER/api-gateway:$IMAGE_TAG -t $ACR_LOGIN_SERVER/api-gateway:latest -f Dockerfile .
          docker push $ACR_LOGIN_SERVER/api-gateway:$IMAGE_TAG
          docker push $ACR_LOGIN_SERVER/api-gateway:latest
          echo "✅ API Gateway pushed successfully"
          
          # Clean up local API Gateway image after push to save space
          echo "Cleaning up local API Gateway image..."
          docker rmi $ACR_LOGIN_SERVER/api-gateway:$IMAGE_TAG $ACR_LOGIN_SERVER/api-gateway:latest 2>/dev/null || true
    
          # Build and push all microservices
          SERVICES=(
            "auth-service"
            "hr-service"
            "attendance-service"
            "payroll-service"
            "crm-service"
            "inventory-service"
            "sales-service"
            "purchase-service"
            "financial-service"
            "document-service"
            "service-management"
            "cpp-service"
            "prescription-service"
            "analytics-service"
            "notification-service"
            "monitoring-service"
            "tenant-registry-service"
            "realtime-service"
          )
          
          for SERVICE in "${SERVICES[@]}"; do
            echo "Building and pushing $SERVICE..."
            docker build -t $ACR_LOGIN_SERVER/$SERVICE:$IMAGE_TAG -t $ACR_LOGIN_SERVER/$SERVICE:latest -f microservices/$SERVICE/Dockerfile microservices/$SERVICE/
            docker push $ACR_LOGIN_SERVER/$SERVICE:$IMAGE_TAG
            docker push $ACR_LOGIN_SERVER/$SERVICE:latest
            echo "✅ $SERVICE pushed successfully"
            
            # Clean up local image after push to save space
            echo "Cleaning up local image for $SERVICE..."
            docker rmi $ACR_LOGIN_SERVER/$SERVICE:$IMAGE_TAG $ACR_LOGIN_SERVER/$SERVICE:latest 2>/dev/null || true
          done
          
          # Final cleanup of Docker cache
          echo "🧹 Final Docker cache cleanup..."
          docker system prune -af || echo "⚠️  Warning: Final cleanup failed"
          
          # Show final disk space
          echo "📊 Final disk usage:"
          df -h / || echo "⚠️  Could not check disk usage"
          
          echo "✅ All images built and pushed successfully"

- stage: Deploy
  displayName: 'Deploy to AKS'
  dependsOn: Build
  condition: succeeded()
  jobs:
  - deployment: DeployToAKS
    displayName: 'Deploy to AKS Cluster'
    pool:
      vmImage: 'ubuntu-latest'
    environment: 'etelios-aks-production'
    strategy:
      runOnce:
        deploy:
          steps:
          - task: AzureCLI@2
            displayName: 'Configure kubectl for AKS'
            inputs:
              azureSubscription: 'Azure-Service-Connection'
              scriptType: 'bash'
              scriptLocation: 'inlineScript'
              inlineScript: |
                # Set variables
                AKS_RESOURCE_GROUP="$(AKS_RESOURCE_GROUP)"
                AKS_CLUSTER_NAME="$(AKS_CLUSTER_NAME)"
                
                # Verify Azure CLI is authenticated
                echo "Verifying Azure CLI authentication..."
                az account show || {
                  echo "❌ Error: Azure CLI is not authenticated"
                  exit 1
                }
                echo "✅ Azure CLI authenticated"
                
                # Get AKS credentials to configure kubectl
                echo "Configuring kubectl for AKS cluster: $AKS_CLUSTER_NAME"
                az aks get-credentials \
                  --resource-group $AKS_RESOURCE_GROUP \
                  --name $AKS_CLUSTER_NAME \
                  --overwrite-existing || {
                  echo "❌ Error: Failed to get AKS credentials"
                  exit 1
                }
                echo "✅ kubectl configured for AKS cluster"
                
                # Verify kubectl connection
                echo "Verifying kubectl connection..."
                kubectl version --client
                kubectl cluster-info || {
                  echo "⚠️  Warning: Could not get cluster info, but connection may still work"
                }
                kubectl get nodes || {
                  echo "⚠️  Warning: Could not get nodes, but connection may still work"
                }
          
          - task: Bash@3
            displayName: 'Generate Kubernetes Manifests (if needed)'
            inputs:
              targetType: 'inline'
              script: |
                # Check if generate-manifests.sh exists and manifests need to be generated
                if [ -f "k8s/generate-manifests.sh" ]; then
                  echo "Generating Kubernetes manifests..."
                  chmod +x k8s/generate-manifests.sh
                  export ACR_NAME=$(ACR_NAME)
                  export IMAGE_TAG=$(IMAGE_TAG)
                  export NAMESPACE=$(NAMESPACE)
                  ./k8s/generate-manifests.sh
                  echo "✅ Manifests generated successfully"
                elif [ -d "k8s/deployments" ] && [ "$(ls -A k8s/deployments/*.yaml 2>/dev/null)" ]; then
                  echo "✅ Using existing manifests in k8s/deployments/"
                  echo "Manifests found:"
                  ls -la k8s/deployments/*.yaml
                else
                  echo "⚠️  Warning: No generate-manifests.sh found and no existing manifests"
                  echo "Manifests should already exist in k8s/deployments/"
                fi
                
                # Verify required manifests exist
                echo "Verifying required manifests..."
                if [ ! -f "k8s/namespace.yaml" ]; then
                  echo "❌ Error: k8s/namespace.yaml not found"
                  exit 1
                fi
                if [ ! -f "k8s/configmap.yaml" ]; then
                  echo "❌ Error: k8s/configmap.yaml not found"
                  exit 1
                fi
                if [ ! -f "k8s/api-gateway.yaml" ]; then
                  echo "❌ Error: k8s/api-gateway.yaml not found"
                  exit 1
                fi
                if [ ! -d "k8s/deployments" ]; then
                  echo "❌ Error: k8s/deployments directory not found"
                  exit 1
                fi
                echo "✅ All required manifests verified"
          
          - task: Kubernetes@1
            displayName: 'Create Namespace'
            inputs:
              connectionType: 'kubernetesServiceConnection'
              kubernetesServiceEndpoint: 'AKS-Service-Connection'
              namespace: '$(NAMESPACE)'
              command: 'apply'
              arguments: '-f k8s/namespace.yaml'
              secretType: 'generic'
          
          - task: Kubernetes@1
            displayName: 'Apply ConfigMap'
            inputs:
              connectionType: 'kubernetesServiceConnection'
              kubernetesServiceEndpoint: 'AKS-Service-Connection'
              namespace: '$(NAMESPACE)'
              command: 'apply'
              arguments: '-f k8s/configmap.yaml'
              secretType: 'generic'
          
          - task: Kubernetes@1
            displayName: 'Apply Secrets'
            inputs:
              connectionType: 'kubernetesServiceConnection'
              kubernetesServiceEndpoint: 'AKS-Service-Connection'
              namespace: '$(NAMESPACE)'
              command: 'apply'
              arguments: '-f k8s/secrets.yaml'
              secretType: 'generic'
          
          - task: Kubernetes@1
            displayName: 'Deploy API Gateway'
            inputs:
              connectionType: 'kubernetesServiceConnection'
              kubernetesServiceEndpoint: 'AKS-Service-Connection'
              namespace: '$(NAMESPACE)'
              command: 'apply'
              arguments: '-f k8s/api-gateway.yaml'
              secretType: 'generic'
          
          - task: Kubernetes@1
            displayName: 'Deploy All Microservices'
            inputs:
              connectionType: 'kubernetesServiceConnection'
              kubernetesServiceEndpoint: 'AKS-Service-Connection'
              namespace: '$(NAMESPACE)'
              command: 'apply'
              arguments: '-f k8s/deployments/'
              secretType: 'generic'
          
          - task: Kubernetes@1
            displayName: 'Apply Ingress'
            inputs:
              connectionType: 'kubernetesServiceConnection'
              kubernetesServiceEndpoint: 'AKS-Service-Connection'
              namespace: '$(NAMESPACE)'
              command: 'apply'
              arguments: '-f k8s/ingress.yaml'
              secretType: 'generic'
          
          - task: Bash@3
            displayName: 'Wait for Deployments'
            inputs:
              targetType: 'inline'
              script: |
                kubectl wait --for=condition=available --timeout=300s deployment --all -n $(NAMESPACE) || true
                kubectl get deployments -n $(NAMESPACE)
                kubectl get services -n $(NAMESPACE)
                kubectl get ingress -n $(NAMESPACE)
