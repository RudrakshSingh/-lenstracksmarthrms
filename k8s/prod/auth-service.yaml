---
# Auth Service Deployment - Production
apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-service
  namespace: etelios-backend-prod
  labels:
    app: auth-service
    version: "latest"
    environment: production
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
      version: "latest"
  template:
    metadata:
      labels:
        app: auth-service
        version: "latest"
        environment: production
      annotations:
        # Istio sidecar injection (namespace-level injection is enabled, but explicit is better)
        sidecar.istio.io/inject: "true"
        # Prometheus metrics scraping
        prometheus.io/scrape: "true"
        prometheus.io/port: "3001"
        prometheus.io/path: "/metrics"
    spec:
      containers:
      - name: auth-service
        image: eteliosacr-hvawabdbgge7e0fu.azurecr.io/auth-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3001
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3001"
        - name: SERVICE_NAME
          value: "auth-service"
        - name: K8S_ENV
          value: "true"
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: etelios-secrets
              key: AUTH_SERVICE_DB_URI
        - name: DB_NAME
          value: "auth_db"
        envFrom:
        - configMapRef:
            name: etelios-config-prod
        - secretRef:
            name: etelios-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3001
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
---
# Auth Service - Production
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: etelios-backend-prod
  labels:
    app: auth-service
    service: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
  - port: 3001
    targetPort: 3001
    protocol: TCP
    name: http

