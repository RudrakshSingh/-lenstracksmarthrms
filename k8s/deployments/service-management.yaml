---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-management
  namespace: etelios-backend-prod
  labels:
    app: service-management
    version: "latest"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-management
  template:
    metadata:
      labels:
        app: service-management
        version: "latest"
    spec:
      containers:
      - name: service-management
        image: etelios-aks-dns-6vulk62q.hcp.centralindia.azmk8s.io/service-management:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3011
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3011"
        - name: SERVICE_NAME
          value: "service-management"
        - name: K8S_ENV
          value: "true"
        - name: API_GATEWAY_HOST
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: etelios-secrets
              key: SERVICE_MANAGEMENT_SERVICE_DB_URI
        - name: DB_NAME
          value: "service_management_db"
          value: "etelios-aks-dns-6vulk62q.hcp.centralindia.azmk8s.io"
        envFrom:
        - configMapRef:
            name: etelios-config
        - secretRef:
            name: etelios-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3011
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3011
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: service-management
  namespace: etelios-backend-prod
  labels:
    app: service-management
spec:
  type: ClusterIP
  selector:
    app: service-management
  ports:
  - port: 3011
    targetPort: 3011
    protocol: TCP
    name: http
