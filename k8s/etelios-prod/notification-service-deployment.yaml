apiVersion: apps/v1
kind: Deployment
metadata:
  name: notification-service
  namespace: etelios-prod
  labels:
    app: notification-service
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: notification-service
  template:
    metadata:
      labels:
        app: notification-service
        version: v1
    spec:
      imagePullSecrets:
        - name: ecr-registry-secret
      containers:
      - name: notification-service
        image: 383234048604.dkr.ecr.ap-south-1.amazonaws.com/etelios-notification-service:latest
        ports:
        - containerPort: 3015
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3015"
        - name: SERVICE_NAME
          value: "notification-service"
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: docdb-credentials
              key: MONGO_URI
        - name: MONGO_USERNAME
          valueFrom:
            secretKeyRef:
              name: docdb-credentials
              key: username
        - name: MONGO_PASSWORD
          valueFrom:
            secretKeyRef:
              name: docdb-credentials
              key: password
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3015
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3015
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: etelios-prod
  labels:
    app: notification-service
spec:
  type: ClusterIP
  ports:
  - port: 3015
    targetPort: 3015
    protocol: TCP
    name: http
  selector:
    app: notification-service
