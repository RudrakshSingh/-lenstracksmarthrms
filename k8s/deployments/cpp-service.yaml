---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: cpp-service
  namespace: etelios-prod
  labels:
    app: cpp-service
    version: "latest"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cpp-service
  template:
    metadata:
      labels:
        app: cpp-service
        version: "latest"
    spec:
      imagePullSecrets:
      - name: ecr-registry-secret
      containers:
      - name: cpp-service
        image: 383234048604.dkr.ecr.ap-south-1.amazonaws.com/etelios-cpp-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3012
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3012"
        - name: SERVICE_NAME
          value: "cpp-service"
        - name: K8S_ENV
          value: "true"
        - name: API_GATEWAY_HOST
          value: "etelios-aks-dns-6vulk62q.hcp.centralindia.azmk8s.io"
        envFrom:
        - configMapRef:
            name: etelios-config
        - secretRef:
            name: docdb-credentials
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3012
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3012
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: cpp-service
  namespace: etelios-prod
  labels:
    app: cpp-service
spec:
  type: ClusterIP
  selector:
    app: cpp-service
  ports:
  - port: 3012
    targetPort: 3012
    protocol: TCP
    name: http
