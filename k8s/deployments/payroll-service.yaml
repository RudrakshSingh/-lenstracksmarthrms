---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: payroll-service
  namespace: etelios-backend-prod
  labels:
    app: payroll-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: payroll-service
  template:
    metadata:
      labels:
        app: payroll-service
    spec:
      containers:
      - name: payroll-service
        image: etelios-aks-dns-6vulk62q.hcp.centralindia.azmk8s.io/payroll-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3004
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3004"
        - name: SERVICE_NAME
          value: "payroll-service"
        - name: K8S_ENV
          value: "true"
        - name: API_GATEWAY_HOST
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: etelios-secrets-prod
              key: PAYROLL_SERVICE_SERVICE_DB_URI
        - name: DB_NAME
          value: "payroll_db"
          value: "etelios-aks-dns-6vulk62q.hcp.centralindia.azmk8s.io"
        envFrom:
        - configMapRef:
            name: etelios-config-prod
        - secretRef:
            name: etelios-secrets-prod
        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: payroll-service
  namespace: etelios-backend-prod
  labels:
    app: payroll-service
spec:
  type: ClusterIP
  selector:
    app: payroll-service
  ports:
  - port: 3004
    targetPort: 3004
    protocol: TCP
    name: http
