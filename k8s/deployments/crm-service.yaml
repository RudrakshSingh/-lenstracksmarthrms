---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: crm-service
  namespace: etelios-backend-prod
  labels:
    app: crm-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: crm-service
  template:
    metadata:
      labels:
        app: crm-service
    spec:
      containers:
      - name: crm-service
        image: eteliosacr-hvawabdbgge7e0fu.azurecr.io/crm-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3005
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3005"
        - name: SERVICE_NAME
          value: "crm-service"
        - name: K8S_ENV
          value: "true"
        - name: API_GATEWAY_HOST
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: etelios-secrets-prod
              key: CRM_SERVICE_SERVICE_DB_URI
        - name: DB_NAME
          value: "crm_db"
          value: "eteliosacr-hvawabdbgge7e0fu.azurecr.io"
        envFrom:
        - configMapRef:
            name: etelios-config-prod
        - secretRef:
            name: etelios-secrets-prod
        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3005
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: crm-service
  namespace: etelios-backend-prod
  labels:
    app: crm-service
spec:
  type: ClusterIP
  selector:
    app: crm-service
  ports:
  - port: 3005
    targetPort: 3005
    protocol: TCP
    name: http
