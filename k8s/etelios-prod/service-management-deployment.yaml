apiVersion: apps/v1
kind: Deployment
metadata:
  name: service-management
  namespace: etelios-prod
  labels:
    app: service-management
    version: v1
spec:
  replicas: 2
  selector:
    matchLabels:
      app: service-management
  template:
    metadata:
      labels:
        app: service-management
        version: v1
    spec:
      imagePullSecrets:
        - name: ecr-registry-secret
      containers:
      - name: service-management
        image: 383234048604.dkr.ecr.ap-south-1.amazonaws.com/etelios-service-management:latest
        ports:
        - containerPort: 3011
          name: http
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3011"
        - name: SERVICE_NAME
          value: "service-management"
        - name: MONGO_URI
          valueFrom:
            secretKeyRef:
              name: docdb-credentials
              key: MONGO_URI
        - name: MONGO_USERNAME
          valueFrom:
            secretKeyRef:
              name: docdb-credentials
              key: username
        - name: MONGO_PASSWORD
          valueFrom:
            secretKeyRef:
              name: docdb-credentials
              key: password
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3011
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3011
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: service-management
  namespace: etelios-prod
  labels:
    app: service-management
spec:
  type: ClusterIP
  ports:
  - port: 3011
    targetPort: 3011
    protocol: TCP
    name: http
  selector:
    app: service-management
