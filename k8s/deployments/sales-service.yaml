---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: sales-service
  namespace: etelios
  labels:
    app: sales-service
    version: latest
spec:
  replicas: 2
  selector:
    matchLabels:
      app: sales-service
  template:
    metadata:
      labels:
        app: sales-service
        version: latest
    spec:
      containers:
      - name: sales-service
        image: eteliosacr.azurecr.io/sales-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3007
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3007"
        - name: SERVICE_NAME
          value: "sales-service"
        - name: K8S_ENV
          value: "true"
        envFrom:
        - configMapRef:
            name: etelios-config
        - secretRef:
            name: etelios-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3007
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3007
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: sales-service
  namespace: etelios
  labels:
    app: sales-service
spec:
  type: ClusterIP
  selector:
    app: sales-service
  ports:
  - port: 3007
    targetPort: 3007
    protocol: TCP
    name: http
