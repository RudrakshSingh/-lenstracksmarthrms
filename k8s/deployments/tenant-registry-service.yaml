---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: tenant-registry-service
  namespace: etelios-backend-prod
  labels:
    app: tenant-registry-service
    version: "latest"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: tenant-registry-service
  template:
    metadata:
      labels:
        app: tenant-registry-service
        version: "latest"
    spec:
      containers:
      - name: tenant-registry-service
        image: etelios-aks-dns-6vulk62q.hcp.centralindia.azmk8s.io/tenant-registry-service:latest
        imagePullPolicy: Always
        ports:
        - containerPort: 3020
          name: http
          protocol: TCP
        env:
        - name: NODE_ENV
          value: "production"
        - name: PORT
          value: "3020"
        - name: SERVICE_NAME
          value: "tenant-registry-service"
        - name: K8S_ENV
          value: "true"
        - name: API_GATEWAY_HOST
        - name: MONGODB_URI
          valueFrom:
            secretKeyRef:
              name: etelios-secrets
              key: TENANT_REGISTRY_SERVICE_SERVICE_DB_URI
        - name: DB_NAME
          value: "tenant_registry_db"
          value: "etelios-aks-dns-6vulk62q.hcp.centralindia.azmk8s.io"
        envFrom:
        - configMapRef:
            name: etelios-config
        - secretRef:
            name: etelios-secrets
        resources:
          requests:
            memory: "256Mi"
            cpu: "50m"
          limits:
            memory: "512Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3020
          initialDelaySeconds: 30
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /health
            port: 3020
          initialDelaySeconds: 10
          periodSeconds: 5
          timeoutSeconds: 3
          failureThreshold: 3
        securityContext:
          runAsNonRoot: true
          runAsUser: 1001
          allowPrivilegeEscalation: false
          readOnlyRootFilesystem: false
          capabilities:
            drop:
            - ALL
---
apiVersion: v1
kind: Service
metadata:
  name: tenant-registry-service
  namespace: etelios-backend-prod
  labels:
    app: tenant-registry-service
spec:
  type: ClusterIP
  selector:
    app: tenant-registry-service
  ports:
  - port: 3020
    targetPort: 3020
    protocol: TCP
    name: http
