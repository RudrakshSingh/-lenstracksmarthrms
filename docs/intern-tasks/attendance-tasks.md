# Attendance Tasks (Intern Ladder)

## L1
1. Add clock-in required-field tests.
2. Add clock-out without open session tests.
3. Add attendance health and list route tests.

## L2
1. Add employee store fallback tests (`store`, `workLocation.storeId`).
2. Add tenant-aware store lookup tests.
3. Add geofence validation edge tests.

## L3
1. Add cross-tenant employee lookup regression tests.
2. Add end-to-end test for clock-in -> realtime event.
